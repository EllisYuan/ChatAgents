# Yuan's Chat Agents

一个以「LLM 应用工程」为核心能力展示的 ReAct agent 项目：evals、trace、token 与延迟可观测、可复现、多模型路由降级。

本文件是术语表，只定义术语**是什么**，不记录实现方式与技术选型——那些属于 `docs/adr/`。

## Language

### 对话

**会话**：
一条公开可见的对话线索，是消息的容器。
_Avoid_: 对话、thread、conversation

**消息**：
会话中一条模型视角的记录——用户输入、助手输出（含其发起工具调用的意图）、或工具返回的结果。
_Avoid_: 聊天记录、chat log、对白

**运行**：
一条用户消息触发的一次完整 ReAct 执行，从接收输入到产出最终回答为止。一次运行可以产出多条消息。
_Avoid_: 请求、轮次、turn、执行

**迭代**：
一次运行内的一轮 ReAct——一次模型调用，加上它触发的全部工具调用。一次运行含一到多次迭代。
_Avoid_: 轮次、步骤、step、turn（前两者分别是「运行」与「跨度」的避用词，复用会歧义）

**运行事件**：
一次运行对外产出的唯一形态——文本增量、迭代边界、工具开始与结束、用量、运行完成或失败。它是领域类型，不含任何线格式（不带 JSON 序列化、不带 SSE 帧头）。落消息、落跨度、推送前端、评测回放，四个下游消费的都是它。
_Avoid_: 流式事件、chunk、SSE 事件（后者专指编码后的线格式）

### 观测

**跨度**：
一次运行内部的一个可观测步骤，例如一次模型调用或一次工具调用。跨度构成树。
_Avoid_: 事件、日志、步骤、step

**用量**：
一次模型调用消耗的输入与输出 token 数。
_Avoid_: 消耗、开销、成本

**用量三态**：
用量的可信度标记：`COMPLETE` 为实测，`PARTIAL` 为流式中断后的估算，`UNAVAILABLE` 为无从得知。三者之外没有第四种状态，且缺失一律不以 0 表示。

**观测事实**：
执行过程中实际发生、事后不可变的物理量——token 数、模型名、时刻、耗时。

**推算**：
由观测事实结合可变配置算出的派生量，典型如成本。推算随配置变化，因此与观测事实分开存放。
_Avoid_: 估算（该词专指 `PARTIAL` 用量的来源）

**Agent 状态快照**：
一次运行的 agent 内部状态留影。它是观测数据，不是恢复源。
_Avoid_: 检查点、checkpoint（该词暗示可恢复，与本概念相反）

### 模型接入

**端点档案**：
一次模型调用所需的完整接入配置——协议、base URL、模型标识、鉴权字段。
_Avoid_: 供应商配置、provider、渠道

**协议**：
模型 API 的请求与响应格式，三者并列且互不翻译：`openai_responses`、`openai_chat_completions`、`anthropic_messages`。
_Avoid_: 厂商、供应商、API 类型

**模型角色**：
一次模型调用在系统中的职责：`main` 负责 ReAct 推理与回答生成，`tool` 负责非主线的辅助调用。两者独立配置，用量分别归属。当前 `tool` 角色**没有调用者**——工具输出摘要已废除，字段与配置为将来的辅助调用（查询改写、结果重排、结构化抽取）预留。但它仍是配置契约的一等公民：服务端预设与用户自定义两层都必须能指定，解析结果不为空，零配置下跟随主模型。
_Avoid_: 主副模型、大小模型

**密钥来源**：
本次调用所用 API 密钥的出处：`user_provided` 为用户在前端自填，`server_default` 为服务端预设。
_Avoid_: 密钥类型、key owner

### 工具

**工具**：
模型可以调用的一项能力，由名字、描述、入参 schema 三者定义。这三者构成工具的身份，属于契约；身份不随背后由谁实际执行而改变。
_Avoid_: 函数、function、插件

**工具供应商**：
为某个工具实际干活的外部服务或本地实现，例如 Tavily 之于 `web_search`、Jina Reader 之于 `web_reader`。同一个工具可以有多个供应商实现，更换供应商不改变工具的身份。
_Avoid_: 渠道、厂商、后端

**工具执行器**：
调用工具的唯一入口，统一施加超时、重试、错误分类、跨度记录与结果渲染。工具本身只写业务逻辑，不含任何横切逻辑。
_Avoid_: 中间件、装饰器、包装层

**外部失败**：
工具因外界原因未能完成——超时、限流、目标不可达、供应商报错。它不抛异常，而是作为工具结果交回模型，由模型决定下一步；同时如实记为错误跨度。
_Avoid_: 异常、报错

**程序错误**：
本项目自身的缺陷或配置缺失——参数校验不过、代码 bug、密钥未配置。它中止运行并上报，绝不伪装成工具结果交给模型。
_Avoid_: 内部错误、系统异常

**渐进披露**：
长文档不整篇注入上下文，而是先返回其结构（标题与各章节清单），由模型按需索取具体章节的读取方式。只做一级，不做多级路由。
_Avoid_: 分页、检索、RAG

### 执行控制

**努力档位**：
用户为一次运行选定的执行强度，取值 `low` / `medium` / `high` / `xhigh`，与 Claude 的 effort 档位同名。它同时决定本次运行的硬上限与软预算。
_Avoid_: 模式、快速模式、深度模式、agent_type

**硬上限**：
执行层强制的天花板，模型无法越过。越限时截到上限并如实告知模型，不静默截断、不报错重来。

**软预算**：
告知模型的额度，模型在其内自行分配。数值可低于硬上限，留出余量，使越限成为异常而非常态。
